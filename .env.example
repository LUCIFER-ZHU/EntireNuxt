# ============================================================
# 环境变量配置示例文件
# 文件路径: .env.example
# 作用: 展示所有需要配置的环境变量
# 使用方法: 复制此文件为 .env.development 和 .env.production，填入真实值
# ⚠️ 警告: 永远不要将包含真实密钥的 .env 文件提交到 Git！
# ============================================================

# --------------------------------------------------------
# 1. 数据库配置 (Supabase)
# 文档: https://supabase.com/docs/guides/database/connecting-to-postgres
# --------------------------------------------------------

# DATABASE_URL: 连接池 URL（用于应用运行时）
# 格式: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
# 获取方式: Supabase Dashboard → Project Settings → Database → Connection string → URI
DATABASE_URL="postgresql://postgres.xxx:password@aws-0-region.pooler.supabase.com:6543/postgres"

# DIRECT_URL: 直接连接 URL（用于 Prisma 迁移）
# 格式: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@db.[REGION].supabase.co:5432/postgres
# 获取方式: Supabase Dashboard → Project Settings → Database → Connection string → URI (direct)
DIRECT_URL="postgresql://postgres.xxx:password@db.region.supabase.co:5432/postgres"

# 说明:
# - Connection Pooler (6543 端口): 用于应用运行时，支持连接池，适合 serverless 环境
# - Direct Connection (5432 端口): 用于数据库迁移，支持会话模式，Prisma Migrate 需要

# --------------------------------------------------------
# 2. 认证配置 (JWT)
# --------------------------------------------------------

# JWT_SECRET: JWT 签名密钥
# 生成方式: openssl rand -base64 32
# 或访问: https://generate-secret.vercel.app/32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT_ACCESS_EXPIRES_IN: Access Token 过期时间
# 格式: 数字 + 单位 (s=秒, m=分钟, h=小时, d=天)
JWT_ACCESS_EXPIRES_IN="15m"

# JWT_REFRESH_EXPIRES_IN: Refresh Token 过期时间
JWT_REFRESH_EXPIRES_IN="7d"

# --------------------------------------------------------
# 3. Nuxt 运行时配置
# --------------------------------------------------------

# NUXT_PUBLIC_API_BASE: 后端 API 基础地址（生产环境）
# 开发环境留空，使用 Nitro 代理
NUXT_PUBLIC_API_BASE=""

# NUXT_PUBLIC_WS_URL: WebSocket 服务器地址
NUXT_PUBLIC_WS_URL="ws://localhost:3001"

# NUXT_PUBLIC_ENABLE_WS: 是否启用 WebSocket
NUXT_PUBLIC_ENABLE_WS="true"

# NUXT_PUBLIC_IMAGE_BASE: 图片资源基础地址
NUXT_PUBLIC_IMAGE_BASE="https://your-cdn.com/images"

# NUXT_BACKEND_IMAGE_BASE: 后台图片资源基础地址
NUXT_BACKEND_IMAGE_BASE="https://admin.your-cdn.com/images"

# NUXT_PUBLIC_SITE_URL: 站点 URL（用于 SEO）
NUXT_PUBLIC_SITE_URL="https://your-site.com"

# --------------------------------------------------------
# 4. Turnstile 验证码 (Cloudflare)
# 文档: https://developers.cloudflare.com/turnstile/
# --------------------------------------------------------

# NUXT_PUBLIC_TURNSTILE_SITE_KEY: 站点密钥（客户端可见）
NUXT_PUBLIC_TURNSTILE_SITE_KEY="0x4AAAAAA..."

# NUXT_TURNSTILE_SECRET_KEY: 密钥（仅服务端使用）
NUXT_TURNSTILE_SECRET_KEY="0x4AAAAAA..."

# --------------------------------------------------------
# 5. 应用配置
# --------------------------------------------------------

# NODE_ENV: 运行环境
NODE_ENV="development"

# APP_VERSION: 应用版本号（构建时注入）
APP_VERSION="1.0.0"

# ENABLE_SOURCEMAP: 是否生成 sourcemap（调试用）
ENABLE_SOURCEMAP="true"

# NUXT_DEV_PROXY_TARGET: 开发环境代理目标（如使用外部后端）
NUXT_DEV_PROXY_TARGET="http://localhost:8080"
